<Window x:Class="MultiDBQ.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:MultiDBQ="clr-namespace:MultiDBQ"

        Title="MultiDBQ"
        Height="350"
        Width="525"
        x:Name="_this">
    <DockPanel LastChildFill="True">
        <StackPanel Orientation="Vertical"
                    DockPanel.Dock="Top">
            <MultiDBQ:SqlConnectionStringBuilder Grid.Row="0"
                                                 x:Name="cnn"
                                                 ConnectionString="{Binding ElementName=_this, Path=Test}" />

            <MultiDBQ:ScriptSelection Grid.Row="1"/>
        </StackPanel>

        <ScrollViewer DockPanel.Dock="Bottom,Right"
                      ScrollViewer.CanContentScroll="True"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <DataGrid x:Name="resourcesGrid"
                      Grid.Row="1"
                      ItemsSource="{Binding ElementName=cnn, Path=DatabaseItems, Mode=OneWay}"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Top"
                      AutoGenerateColumns="True">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn x:Name="resourceCheck" />
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
    </DockPanel>
</Window>
